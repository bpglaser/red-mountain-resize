use image::DynamicImage;

pub struct Shrinker {}

impl Shrinker {
    pub fn new(image: &DynamicImage) -> Self {
        unimplemented!()
    }

    pub fn calculate_removal_points(&mut self, distance: u32) -> Vec<(u32, u32)> {
        unimplemented!()
    }
}
